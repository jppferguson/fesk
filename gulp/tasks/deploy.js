'use strict'

// import config      from '../config'
import gulp        from 'gulp'
import handleError from '../helpers/handle-error'

gulp.task( 'deploy', [ 'prod' ], function() {

  // Run deployment here
  handleError( 'Deploy task hasn\'t been setup yet...' )

} )
